# dsh-doctor 项目交接清单（2026-08-15 晚间 · 完整版）

> **新对话从这里开始**：先读本文件。这是跨会话完整交接——当前状态、进行中事项、流程约定、历史索引都在这里。
> 上一会话（2026-08-15 全天）完成了大量工作，本文件已按"新会话可独立接手"重写。
> 📄 **2026-08-16：论文相关工作已拆出至 [`论文交接清单.md`](论文交接清单.md)**（arXiv 提交 + B 档论文），本清单只含 dsh-doctor 工具相关事项。
> 🛠 **2026-08-16 会话**：✅ 层 C 观察者落地（设计重建 + MVP + 23 测试）、✅ dshmarket 快照核实（无需 PR）、🔧 E10 dsh 识别修复——详见三-4。
> ⚠️ **2026-08-15 晚间：整个项目从 `/Users/waterfly/收藏` 迁移至 `/Users/waterfly/dsh工作区/`**（含散落文件与 d1724 调查残留）；profile 的 file: 依赖已更新指向新路径并重刷硬链接（inode 一致验证通过）。

---

## 一、项目身份

- **工具**：`dsh工作区/dsh-doctor` 仓库，真身在 `plugin/dsh-doctor.mjs`（CLI 与插件共用同一源码）；仓库根 `dsh-doctor.mjs` 是薄封装
- **GitHub**：https://github.com/moonquake2004/dsh-doctor（公开，MIT，当前 HEAD `ec01f44`，已同步）
- **npm**：`@moonquake2004/dsh-doctor`，**latest = 0.3.4**（版本链 …→0.3.2→0.3.3→**0.3.4**；0.3.4 = P12 skip 精修 + patch name/client 注册 scoped 修复（#16 发布级 bug），**已发布，npm 安装路径可用**——此前 0.3.0~0.3.3 因裸名 patch/client 注册，npm 安装 boot 失败）
- **社区主帖**：deepseek-ai/deepseek-harness **discussion #1534**（该仓库 issues 已禁用，全走 Discussions）
- **作者真名**：Xiaoji Zhou（论文/arXiv 用）

---

## 二、工具现状（0.3.2+，92/92 测试全绿）

### 检查清单（31 项 = 26 内置 + 5 目录）
- **env 7 内置**：E1 命令 PATH / E2 .env 类型 / E3 node 版本 / E4 node-pty / E5 存储 JSON / **E6 锚点 tripwire**（S6/S7/S10 契约防腐烂）/ E10 端口 3080
- **profile 11 内置**：P1 bundle 解析 / **P2 重复 entry id**（#1404）/ P3 patch name 解析 / P4 file: 依赖 / **P5 双实例遮蔽**（#1486/#1697）/ **P7 patch YAML lint**（#1724）/ **P8 adapter 冲突**（#1904②）/ **P9 settings inject**（#1904⑤）/ **P10 客户端服务注入**（#1947）/ **P11 main 产物缺失**（#1965）/ **P12 bundle 版本 vs CLI（#1719 v1.1 installed_bundle，warn）**
- **session 8 内置**：S1 孤儿调用 / S2 未闭合 turn / S6 seq 连续性 / S7 end-seed 重放 / S8 未知类型 / S9 zstd 帧数 / S10 sourceEventSeqs / S11 全会话扫描
- **目录 5 条**（层 A，声明式数据）：E7 dsh-in-path / E8 .npmrc 开关(warn) / E9 workspace.json / P6 name 空格 / E11 settings.yaml 可写
- 测试：**43 fixtures + 26 catalog + 23 observer = 92**；fixtures 隔离属性（该响的响、不该响的不响）

### 架构（三层 + 契约）
- **层 A 远程检查目录**：`plugin/checks.json` 声明式规则（规则是数据，只读探测原语：command-exists/path-*/json-valid/text-*/file-size/glob-count/file-writable）；拉取链 新鲜缓存(6h TTL)→raw.githubusercontent→last-known-good→内置；新检查零发版分发
- **层 B 自更新**：`checkForUpdate`（npm dist-tags vs 本地）；`--update` 跑宿主 profile pnpm install；`DSH_DOCTOR_AUTO_UPDATE=1`；`--no-catalog` 禁网络
- **层 C 半自动 LLM 观察者（2026-08-16 落地）**：`--observe` / `--observe-llm` / `--observe-apply`，见 `docs/layer-c-observer.md`（设计重建 + MVP：聚类→确定性草稿→LLM 富化→人审→本地覆盖层 `checks.local.json`）
- **v1 信封**：`--json --envelope` 输出 `dsh-doctor/v1`：`{schema, tool, generatedAt, profile, exitCode, summary{pass,warn,fail}, ok, checks:[{name,status,detail}]}`，小写 status，退出码 0/1/2；契约文档 `docs/doctor-contract.md`（含最小兼容子集）
- `--profile` 支持直接传目录路径；npm bin `dsh-doctor` 可用

### 部署
- 用户 profile `~/.dsh/profiles/web`：**file: 依赖 + pnpm 硬链接** → 仓库 plugin/ 改动即时同步，无需 pnpm install；运行中服务 PID 需重启才换 host 侧代码（但 /dsh-doctor/run 每次 spawn CLI 文件，API 永远跑最新引擎）
- dshmarket 已升 **1.5.0**（本机已验证，快照 2026-08-15 已含我们）；pnpm 9.15.9；profile .npmrc 有 `ignore-workspace-root-check=true`

---

## 三、进行中事项（按优先级）

### 🟡 1. #1846 check-lifecycle 互审（进行中：我们已回审读意见，等 worm-ai 回）
- 已发：`docs/check-lifecycle.md`（草案：检查即数据/自省/认证门禁/进化回路 + 检查条目 schema）→ #1846 帖内
- ✅ **2026-08-16：worm-ai 的 6 项 probe 检查项草案已贴出（第 67 代，`dsh-defect.*`，tool=dsh-host-defects，v1 契约零改动兼容）**，见 #1739
- ✅ 我们已发布审读意见（#1739 discussioncomment-18032265）：6 项里仅 `subagent-bare-emit` 可直接落成会话日志文本探针，其余 5 项为源码语义审计 → 建议签名探针（text-contains 对已装包源码）；并提议把 `isPromise-native-only` / `webserver-disposer-replay` 两项填进 probe/anchor/fixtures/certifiedOn 作 worked example（anchor 对齐 0.1.0-rc.6）
- ✅ **2026-08-16（当天）：worm-ai 连发两条**——① worked example（Entry 1 isPromise-native-only 签名探针 status=pass + **Entry 2 webserver-disposer-replay status=warn 未认证** + 状态同步 writableRoot G5 已修 + Q1-Q3）；② **上游 patch 草案**（G4 修复的上游可采纳形态：packages/core/agent/src/index.ts，删 isPromise import + duck-type + Promise.resolve 收养，含行为对照表与测试建议；因 CONTRIBUTING 不接受外部 PR 而发 diff 供 cherry-pick）→ **我们对照上游 master 源码独立验证后已发布完整回复**（discussioncomment-18036542，旧验证帖 18036496 已由用户删除）：import 行 11 精确命中、缺陷块实际 654-668（他引 651-667 差几行）、源码↔编译产物对应确认（src 654-668 == lib 751-760）、修复逻辑与测试建议验证通过、CONTRIBUTING 原文核实；**train 语义闭环提议：patch 落地即认证 Entry 1**（pre/post 源码片段即 fixture 对，走层 C --observe-apply 本地覆盖层→目录）；Q1（签名探针双 needle 可用 compound 或给 text-contains 加 absent 字段）、Q2（fixtures 需可运行对）、Q3（warn 可进目录但认证是发布硬门槛）已答
- ⏭ 后续：① 等 worm-ai 回（patch 上游采纳动向 + 我们认证 Entry 1 的提议）；② 可选：起草我们的签名探针示例条目（isPromise 用他给的 bad/fixed 片段即可）；③ 等 worm-ai 审我们的 check-lifecycle 草案
- ✅ **2026-08-16（当天更晚）：worm-ai 连发两条**——① gen-74 诚实再审：`subagent-followup-cold` 与 `subagent-bare-emit` 均撤回为 **by-design（源码注释证明删除条目是防双活机制、失败释放是防祖先悬挂）**，教训=发布状态前先对源码复验（认证门禁拦截"意见变检查"）；② **clientManifest 修复方案（per-page slots）征求我们意见**：宿主 inspect-registry.ts:76-85 无条件替换单槽 + 客户端 index.ts:191 全量同步（两处均已核实属实）→ **我们已回（discussioncomment-18036932）**：typert 设计文档证实 Remote 业务契约无 page 身份（"Scope identity does not enter the business method's parameters"；gateway invoke 只收 {namespace,method,args,signal}）→ pageId 必须走线；无断开钩子接进 registry，但客户端 connection/reset 已重发布=重载即重同步；**建议可选 pageId 参数（缺省=匿名单槽旧行为不变），故障模式从"一页覆盖另一页"降级为"ghost 直到重载"**；落地后该条目即可 fixture 化（pre/post 宿主源码为可运行对）
- **2026-08-16（当天最晚）：worm-ai gen-81 收口**——`webserver-disposer-replay` 再审为 **by-design**（webserver/src/index.ts：register/registerUpgrade 同 key 重复即 throw、disposer 按 key 删；tapIndex 按引用删；registerFallback 单席位二次 throw），**8 项宿主缺陷审计全部收口：5 修（G2-G5 + clientManifest G6 gen 79）+ 3 by-design + 0 open**；clientManifest union-merge 边界已文档化（无 page 身份时 ghost 条目，page-slotted 方案等 wire page 身份）→ **我们已独立复核源码并回（discussioncomment-18040157）**：判定属实，补一个诚实边角（删后重注册同 path，旧 disposer 可能删新主人路由——任何 keyed registry 的标准 stale-disposer 风险，非 registry 缺陷）；指出 3 个 by-design 正是认证门禁的成果（不复验就会全以 warn 出货）
- 备注：#1739 另几条新回复（Electricitysheep 手册收录 dsh-diagnose / xu-kai-quan 的 dsh-tool-diagnose / guiyi-labs 的 kubemd）与 #1719/#2230 相关帖——**用户指示暂不回，搁置**

### 🟡 2. #1719 信封映射表（等对方）
- zoahdev 承诺起草 `--env` JSON 互操作映射表（E1/E3/E4 ↔ 他的 node/pnpm/dsh-path/port）——**仍未发（8/16 核实：评论区无、README 无、新帖 #2416 无关）；用户决定不再催**；该表价值已被 r5 词汇表实质覆盖（docs/doctor-contract.md 语义表），他若自发仍镜像 README，但不主动等
- 三家已对齐 v1 信封：dsh-doctor / dsh-plugin-doctor(v1.6+) / dsh-diagnose(--doctor-json 小写化)；**ciceroyang/dsh-doctor 0.3.0 也采纳信封（0.4.0 新增多帧 zstd 会话抽查，13/13 测试）——2026-08-16 我们已回重叠对照（S9/S11 已覆盖帧健康），discussioncomment-18032304**
- **2026-08-16（当天）：ciceroyang 提案信封 v1.1 检查名词汇表（建议 S9/S11 → log_health 统一 name，他起草增补、zoahdev 过目）→ 我们已回（discussioncomment-18036881）**：① 方向 +1，但反对把 S9/S11 压进一个 log_health（S9 帧结构 error 类 #1043 vs S11 完整性/隔离/堆 warn 类 #1550 语义不同）——帧健康条目共用，完整性扫描留 tool-local；② 词汇表条目 = check-lifecycle 已认证跨工具检查层（语义定义 + status 词汇 + 讨论号锚点）
- **词汇表已定稿 r5（第 4 家 sjh9714/dsh-win32 加入）**：status ∈ {pass,warn,fail,skip}（pass 胜出、顶层 ok 布尔保留）；skip 必须带 reason、summary.skip 常驻；退出码 0/1/2 归 CLI 入口、采纳属破坏性变更；node 两态（pass=满足 ^22.19.0||>=24.0.0，warn=其他，无 fail<18 民间阈值，EBADENGINE 语义）；pnpm 缺失=warn（corepack 可恢复）；**#2259** 请求上游把 root package.json 已声明的 engines 传播进发布包
- **2026-08-16 我们已做 r5 对齐并发布确认（discussioncomment-18039044）**：E3-node 两态、E1-pnpm 改 warn、summary.skip 常驻、README r5 兼容行+归属（commit 957c16b，0.3.1）；契约文档 docs/doctor-contract.md 更新为 r5（含词汇表语义表）
- **r5 已正式冻结（ciceroyang 宣布，discussioncomment-18039070）**：三家符合（ciceroyang 0.5.0 / 我们 0.3.1 / dsh-win32 v0.9.3）；sjh9714 自曝 v0.8.x node 实为 pass/fail、v0.9.3 才修正并附 receipts；**sjh9714 提出 v1.1 候选 `installed_bundle`（profile 内 bundle 版本 vs 运行 CLI 版本差）→ 我们已回（discussioncomment-18040197）：我们正是第二个 CLI+bundle 双形态实现（Layer-B 只比 npm latest vs 运行模块，profile 内 bundle 落后会漏），提名 v1.1 词汇项 `installed_bundle`，愿合稿，不阻塞 r5**；**sjh9714 三条合稿意见（四态分析/manifest-lies 状态/age-gate 暂缓）→ 我们已按此对齐 P12（commit 7d4e760，0.3.3：新增 manifest 声明但 node_modules 缺失=warn、分歧 detail 补年龄门提示）并回合稿意见（discussioncomment-18040329）**；**sjh9714 再提 skip 修正（未声明=无对比对象，pass 会让 CI 误判"已同步"，git_bash 同形）→ 采纳并落地（commit c98ef30，0.3.4：引擎 reportSkip 支持真实 skip 状态，r5 后首次触发；P12 四态最终：未声明=skip/撒谎=warn/一致=pass/分歧=warn），已回（discussioncomment-18040375，条目文本 final，等 r6/v1.1 表发布后认领）**
- 待办：zoahdev 表态（--env 映射表仍等）；0.3.3 已发布 npm ✅；#2259 上游跟进（E3 将来可读安装包 engines 自验证）；**v1.1 `installed_bundle`：P12 四态+skip 已落地并随 0.3.4 发布（commit 7d4e760 + c98ef30 + 44e4f74）；2026-08-17 会话（迁移后）：已本地认领——P12 信封检查名 `P12-bundle-version` → `installed_bundle`（引擎 report id + catalogSeverity key + fixtures 断言 + 契约文档词汇表四元组条目 + 4 份 README，0.3.5 未发布待发布），94/94 测试全绿；r6/v1.1 增补帖草稿已拟（含四态表 + 两家实现认领 + ciceroyang 弃权 + 厂商前缀过渡）——**用户决定暂不发布，草稿在 /tmp/r6-v1.1-sheet-draft.md，待用户确认后发**（sjh9714 在 discussioncomment-18040197 已等我们发帖；ciceroyang 的 docs/contract-v1.md 链接在 main 上 404，其仓库尚无此文件）**

### 🟡 3. ⚠️ token 轮换（安全）
- bypass-2FA granular token（`npm_N0u3...`）明文出现在会话 + ~/.npmrc 无发布权限 token——**建议用户去 npm 后台轮换**；轮换后下次发布需新 token
- **2026-08-16：已用该 token 发布 0.3.0~0.3.3（用户提供旧 token，发布后即删 publish.npmrc）**——轮换仍建议尽快做（token 已在会话明文多次）
- **2026-08-16 安全审计发现**：`~/.npmrc` 与 `~/.dsh/.npmrc` 存在多个 npm token（`_authToken` npm_Dxp... + bypass token）——建议一并清理/轮换
- **2026-08-16 Noema 修复**：根因 = @zseven-w/dsh-noema 的可选平台二进制（darwin-arm64）被 pnpm 选择性安装漏掉；已通过 `pnpm add @zseven-w/dsh-noema-darwin-arm64@0.1.0-rc.1` 显式装为 profile 顶层依赖修复（noema_status ok）。注意 pnpm 默认不装传递性可选依赖
- **2026-08-17 P5 修复（第一轮）**：P5 报顶层 `@deepseek-ai/{cosmokit,schemastery}` 真实目录重复（#1486/#1697 双实例风险）——是上一行 Noema 修复 hoist 上来的；已把它们改为指向宿主 npx checkout 的 symlink（#1697 workaround），P5 转绿。⚠️注意：下次完整 `pnpm install` 可能把真实目录 re-hoist 回来（pnpm 管 node_modules），届时需重做；临时避坑勿在 profile 跑无参数 `pnpm install`
- **2026-08-17 P5 复发（第二轮，当前状态 green）**：重启 DSH 后宿主 npx checkout 从 `1e7f6d9597241db0` 变为 `b86ed90107c62dab`（活跃宿主判定：PID 的 native 依赖如 sharp/koffi/node-pty 从该 checkout 加载），profile 里两个 symlink 仍指向旧 `1e7f6d…` → doctor P5 再次标红。已把 `cosmokit`/`schemastery` 的 symlink 重新指向 `b86ed90107c62dab`（版本一致，均 1.8.2/3.18.1），P5 复绿、全部通过。⚠️ **通用结论：每次重启 DSH（宿主 npx checkout 可能变动）后，若 doctor 报 P5，先检查 profile 顶层 `@deepseek-ai/*` symlink 是否还指向当前活跃宿主**；活跃宿主可用 `lsof -p <PID> | grep npx` 看 native 依赖路径确认

### 🟢 4. 可选待办
- ✅ **dshmarket 离线快照——已核实无需 PR（2026-08-16）**：本机已升 1.5.0，快照（updated 2026-08-15，457 条）与线上 registry（701 条）均含 `moonquake2004/dsh-doctor#plugin`；dsh-market 每次运行拉 awesome-dsh-plugin.com/plugins.json（1h TTL），离线才回退快照。唯一残留：市场描述仍显示"19 checks"（上游站点日构建自动从 README 重扫，等下次 23:02 UTC 构建刷新即可，无需动作）
- ✅ **层 C（半自动 LLM 观察者）——2026-08-16 已落地**：设计重建于 `docs/layer-c-observer.md`，MVP 实现 `plugin/observer.mjs` + CLI（`--observe` / `--observe-llm` / `--observe-apply`）+ 23 项测试；本地覆盖层 `plugin/checks.local.json`（未认证不随包分发）
- 附带修复：**E10 dsh 识别**——`portOccupierInfo` 的 ps 命令串匹配在 npx/pnpm 安装形态下漏判（运行中的 dsh web 被误报"被其他程序占用"），新增 lsof cwd/txt 兜底（`.npm/_npx/`、`node_modules/@deepseek-ai/` 签名）；fixtures 的 E10 两条用例（空闲 PASS / 非 dsh 占用 FAIL）回归通过

---

## 三之① 自助排障：dsh web 起不来 / 诊断面板缺失（重要）

**症状 A：报 `client-modules: bundle .../client.js loaded without registering "@moonquake2004/dsh-doctor" via __ModuleLoader__.load` → dsh web boot 崩溃**
- 根因：plugin/client/client.js 第1行 `__ModuleLoader__.load({id: ...})` 的注册 id ≠ 当前 bundle 身份（裸名 dsh-doctor vs scoped 安装）。scoped 身份下 loader 找不到注册 → client-modules 崩 → web 起不来。
- 自助修（3 步）：① 把 client.js 第1行 id 改成 = package.json.name（`@moonquake2004/dsh-doctor`）② 重启 dsh web ③ `git commit`（否则 checkout 后退回裸名复发）。

**症状 B：设置→诊断面板没有 dsh-doctor**
- 先 `dsh --profile web --dump-config | grep moonquake` 确认插件在层栈（在=挂载 OK）；若插件在但面板缺，多半是运行进程用旧配置 → **重启 dsh web**。
- ⚠️ 重启会断开当前 GUI 会话（#1904 同坑）——先保存再重启。

**通用教训**：插件里所有"以包身份注册"的点必须 = npm 包名：① `cordis.patch.yml` 的 `insert.name` ② `client.js` 的 `__ModuleLoader__.load` id。任一处裸名 × scoped 安装 = boot 崩或面板缺。

## 四、流程约定（重要）

- **所有对外回复**（GitHub discussions 等）：**先给用户看草稿 → 用户确认 → 用英文发布**
- 验证类结论发布前必须独立复核（本会话多次教训：子串误报/OCR 误读/首匹配误报）
- 社区讨论引用格式：讨论号即数据标识（如 #1404）

---

## 五、用户环境备忘

- 双输入法：豆包 DoubaoIme + 微信 WeType（IME 卡死可换输入法恢复）
- 会话很大（~53 万事件/18MB），S11 报超大警告属正常
- 本机沙箱：旧工作区为 `/Users/waterfly/收藏`，**项目已迁移至 `/Users/waterfly/dsh工作区/`**（写入需升级权限）；写 `~/.dsh`、`/opt/homebrew` 等需升级权限（已获批过多次）
- npm 本机缓存坏（root 权限）→ 发布/安装用 `--cache /tmp/npm-cache`；tesseract 读 /tmp 文件会失败（复制到工作区即可）
- tectonic 已装（TeX 编译用 `TECTONIC_CACHE_DIR=/Users/waterfly/dsh工作区/.tectonic-cache`）
- fork：moonquake2004/awesome-dsh-plugin（PR #278 已合并，可删）

---

## 六、常用命令

```bash
node /Users/waterfly/dsh工作区/dsh-doctor/dsh-doctor.mjs --json   # 全量诊断 JSON
node --test plugin/test/fixtures.mjs plugin/test/catalog.test.mjs plugin/test/observer.test.mjs   # 87 项测试
#   （本机 node v22.22.1 上 `node --test plugin/test/` 目录形式报 MODULE_NOT_FOUND，用显式文件列表）
# 层 C 观察者：
node dsh-doctor.mjs --observe run.json                          # 聚类 + 确定性草稿 + prompt
node dsh-doctor.mjs --observe run.json --observe-llm "<cmd>"    # LLM 富化（cmd 读 stdin 写 stdout；或设 DSH_DOCTOR_LLM_CMD）
node dsh-doctor.mjs --observe-apply proposals.json              # 校验通过并入 plugin/checks.local.json（幂等）
# 发布：cd plugin && npm publish --cache /tmp/npm-cache --userconfig /tmp/publish.npmrc
#   （publish.npmrc 临时文件含 bypass token，用完即删）
# registry：curl https://registry.npmjs.org/@moonquake2004%2Fdsh-doctor | jq .dist-tags
```

---

## 七、本会话生态成果速查（社区讨论闭环记录）

| 讨论 | 状态 | 我们做了什么 |
|---|---|---|
| #1534 | 主帖 | dsh-doctor 发布帖 |
| #1404/#1486 | 根因源 | P2/P5 检查 |
| #1614/#1051 | 已回 | IME 实证 / composer 三缺陷 |
| #278(市场 PR) | ✅ 合并上线 | 市场可见，npm 0.2.2+ |
| #20(dsh-market) | 上游修复 | 1.4.0 已装；PR #23 评估留评 |
| #1697 | 已回 | P5 symlink 感知 + 协议守卫评论 |
| #1719 | 已回多轮 | 信封契约收敛、E10、tool 字段；**2026-08-16：ciceroyang 0.4.0 多帧 zstd 抽查 → 我们已回 S9/S11 重叠对照（discussioncomment-18032304）**；zoahdev 的 --env 映射表仍等 |
| #1724 | 已回更正 | P7（含顶层映射+序列混排真实机制） |
| #1739 | 已回多轮 | 症状映射表 + check-lifecycle 草案互审；**2026-08-16：worm-ai 贴 6 项 `dsh-defect.*` 草案（第 67 代）→ 我们审读已回**（含签名探针建议 + 2 项 worked example 提议） |
| #1846 | 已回 | RFC 贡献 + check-lifecycle 地基建议；草案互审进行中（worm-ai 已贴 6 项，我们已审回） |
| #1904 | 已回 | P8/P9 落地；**2026-08-16：Electricitysheep 邀请整理启动/重启问题集进 dsh-handbook 第 3 章 → 已发布完整 checklist（7 类条目带归属，discussioncomment-18032432），待他回（要 PR 还是 inline）** |
| #1947 | 已回 | P10 落地 |
| #1965 | 已回 | P11 落地 |
| #1977 | 已回 | P2/P9 现场验证 |
| #2230 | 已回(中文) | ERR_PNPM_IGNORED_BUILDS(node-pty) 帖——核实 `dsh plugin approve-builds` 是 pnpm 交互式转发（CI 会挂），主推官方 allowBuilds 非交互修法 + git 源 key 需 commit hash(#12367) + E4 验证；**2026-08-16 已回中文（discussioncomment-18037287）** |
| #2402 | 已回多轮(英文)
| #2579 | 已回(英文) | Node v18.20.1 boot 失败（node:util.parseEnv 是 Node20.12 才加的，18 加载即崩；且远低于官方 engines ^22.19||>=24 底线）＝我们的 E3-node / r5 node 语义覆盖类 → 已回：升 Node 22.19+，提 doctor 可先检出；**discussioncomment-18045282** |
| #1719(dshbase) | 已回(英文) | sjh9714 澄清 dshbase「dsh-doctor」页=我们镜像（实为版本落后页，曾被 ciceroyang 误当第 4 实现，已更正计数回 3）；我们 README 过时数字(20)是传染源→已修 26 内置+5 目录=31 并发布澄清（**discussioncomment-18046400**）；mohitmathur95 宣布要加 installed_bundle（潜在第 3 实现同检查，vendor 前缀路线=我们 P12 同款） |
| #2088+testkit | 已参与+已修bug | dsh-testkit 设计伙伴运行（issue 16）抓出**发布级 bug**：cordis.patch.yml 用裸名 name dsh-doctor，npm 安装路径 loader 无法解析→boot 失败（load-level 已核实，二次复现）。已修：patch name→scoped @moonquake2004/dsh-doctor + 回归测试(94/94) + dev profile 依赖 key/bundles 同步 scoped + @deepseek-ai symlink 指回 live host；已回 iiwish(issuecomment-5312722000) 请其 rerun。**npm 安装路径此前一直装不上（release-blocker）** |
| #2088+testkit | 已回 | **kernel-guard 提案帖（discussioncomment-18048803，2026-08-16 发布）**：实名链→kernel 无自愈（reconcilePlugins 仅 pnpm 成功后维护 composition，不碰 patch inserts、无备份/回滚；loader 对不可导入 entry 硬 throw；parsePatchList 合并无备份）→ 提议①loader 隔离 failing entry（log+隔离建议，而非硬崩整个 boot）②boot 前身份预校验（patch insert.name/客户端注册 id == 已装包名，响亮但可恢复地失败）③#1719 契约加约定=插件身份注册点(name/id)==npm 包名；愿展开 patch 草稿。**这是 #16 发布级 bug 对上游 release-gate 论点的实证反馈** |
| #2088+testkit | 已参与 | dsh-testkit（iiwish，真实宿主生命周期测试）对比表漏我们 → issue 16 已回已发评论（**issuecomment-5311381548**：接受 scope/作者侧 vs 用户侧分列/不动 schema/0.3.3 定设计伙伴对象）+ docs PR 已开（**iiwish/dsh-testkit#17**：README 对比表拆 author/user 侧 + README.zh-CN 镜像 + community-validation 加 exact-host-state 交叉引用，见下） |
| #2402 | 已回多轮(英文) | macOS exFAT 上 lefthook 锁 inode 校验必挂——核实属实 + 降级方案 +1（18040427）；作者回（探测须同挂载点，外置盘不稀有）→ 已回补充（每进程探测一次缓存结论 + 推广 FUSE/网络盘 + 提议出 diff 供 cherry-pick，**18040629**） |
| #2385 | 已回(中文) | Windows 用户 boot 崩 `duplicate loader entry id: mcp-argo` = #1404 失败类（P2 检查）→ 已回定位步骤 + 三种成因修法（discussioncomment-18040302） |
| #1928/#1929 | 未回(记录) | exec 误报教训 |
| #1415 | 用户参与 | 7 检查矩阵验证 + 多轮交流 |

**生态工具**：zoahdev/dsh-plugin-doctor（作者侧+profile-shadow）、worm-ai/dsh-diagnose（症状诊断）、ciceroyang/dsh-doctor、boyin111-1/dsh-doctor（兄弟实现）——三家已对齐 v1 信封契约

---

*历史明细（早期 19→30 项检查演进、层 A/B 实现过程、三轮误报消除、论文写作全程）见 git log 与各 commit message，不再赘述。*
